# deploy/compose.prod.yaml
# Production override — bind mounts to /data/*, CPU pinning
#
# Usage:
#   podman-compose -f compose.yaml -f compose.prod.yaml up -d

services:
  barter-server:
    cpuset_cpus: "0-1"

  barter-features:
    cpuset_cpus: "2-3"

  nautilus:
    cpuset_cpus: "2-3"

  jupyter:
    cpuset_cpus: "2-3"

volumes:
  parquet-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /data/parquet
  ipc-socket:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /data/ipc
  features-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /data/features
  checkpoints-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /data/_checkpoints
  catalog:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /data/catalog
